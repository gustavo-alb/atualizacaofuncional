<%= simple_form_for @classe, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nome %>
  <%= error_span(@classe[:nome]) %>
  <%= f.input :codigo %>
  <%= error_span(@classe[:codigo]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                administracao_classes_path, :class => 'btn btn-default' %>
<% end %>

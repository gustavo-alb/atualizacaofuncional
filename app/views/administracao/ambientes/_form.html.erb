<%= simple_form_for @ambiente, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nome %>
  <%= error_span(@ambiente[:nome]) %>
  <%= f.input :codigo %>
  <%= error_span(@ambiente[:codigo]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                administracao_ambientes_path, :class => 'btn btn-default' %>
<% end %>
